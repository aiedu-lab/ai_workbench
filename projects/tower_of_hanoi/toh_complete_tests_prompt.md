# Prompt: Tower of Hanoi — Complete Test Implementations

## Context

You are working in `src/tests/`. The test skeleton has been
generated by `toh_define_tests_prompt.md`. Every test method
currently raises `NotImplementedError`. The class scaffold in
`src/` has `raise NotImplementedError` bodies — the tests will
fail until the classes are implemented, but the assertions
themselves must be syntactically and logically correct.

---

## Objective

Fill in all test method bodies with assertions. Work one file at
a time. After completing each file, run:

```bash
python3 -m pytest tests/test_<class>.py --co -q
```

to confirm the test collection succeeds (no syntax errors) before
moving to the next file.

---

## Test Requirements

| File | Must assert |
|---|---|
| `test_disc.py` | valid size stored; zero/negative raise `ValueError`; `Disc(2) == Disc(2)`; `Disc(1) < Disc(3)` |
| `test_tower.py` | empty tower `is_empty()`; full tower size; `peek()` returns top without removing; `pop()` on empty raises `IndexError`; `push()` larger-on-smaller raises `ValueError`; `as_size_list()` order; `display()` produces stdout output |
| `test_move.py` | `is_solved()` False at start; `next()` returns True while moves remain; `next()` returns False when solved; Tower[2] holds all discs in order; move count equals 2ᴺ−1 |
| `test_orchestrator.py` | Tower[0] starts with all discs; Tower[1]/[2] start empty; after `run()` Tower[2] full and Tower[0]/[1] empty; step file exists and is non-empty; move count = `## Step` heading count in file |
| `test_integration.py` | parametrize over N=1,2,3; Tower[2] final state matches `range(N,0,-1)`; move count = 2ᴺ−1; at every intermediate step each tower is strictly descending (largest disc at bottom) |

---

## Constraints

- Do not modify class implementations or conftest.py.
- Use `pytest.raises` for exception assertions.
- Use `tmp_path` fixture for any file-writing tests.
- Use `capsys` for stdout capture in `display()` tests.
- Use `@pytest.mark.parametrize` for the integration tests.
- 2-space indentation; line length ≤ 79 characters.
