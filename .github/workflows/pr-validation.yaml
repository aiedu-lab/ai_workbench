# STUB (mock, pending ai_workbench's real bazel codebase): this repo
# has no bazel build/test targets yet. This workflow exists so
# //:pr_check (bazel run //:pr_check) has something real-but-trivial
# to wrap via `act` locally. Replace the placeholder step below with
# a real check (`bazel build //...`, etc.) once ai_workbench's
# codebase lands. See the ITDev SDD Step 2.10 cross-repo sync that
# created this stub (mirrored from aim's identical bootstrap).
name: PR Validation

on:
  pull_request:
    branches: [main]

jobs:
  validation:
    name: PR Validation
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder check
        run: 'echo "TODO - no real validation wired up yet"'
